# Phase 10 — Reports + Impact Tracking

> **وضعیت (۲۰۲۶-۰۸-۱۵):** گزارش تأثیر پس از انتشار (D-019) تحویل شد — فقط دادهٔ واقعی GSC، بدون جعل عدد.

- [x] P10-01 Schema: impact events, reports (گزارش محاسبه‌شونده از `gsc_page_metrics`؛ baseline/observed دو پنجره)
- [x] P10-02 Change baseline / outcome tracking — `BuildPublishImpactReport` (قبل/بعد انتشار: جایگاه/کلیک/نمایش + وضعیت improved/declined/stable + سری روزانه برای نمودار)
- [x] P10-03 Report builder and executive projections — `BuildContentImpactSummary` (کارت داشبورد «تأثیر محتوا»: تعداد، توزیع وضعیت، بهترین/ضعیف‌ترین بهبود، فهرست افت‌ها)
- [x] P10-04 Admin + Client report UI — ویجت تأثیر در جزئیات بازبینی (با VTrendChart) + کارت داشبورد + جزئیات در «تغییرات اجرایی»
- [x] P10-05 QA — `PublishImpactReportTest` (Feature) + دادهٔ واقعی در Preview + `gsc:import --sync`
