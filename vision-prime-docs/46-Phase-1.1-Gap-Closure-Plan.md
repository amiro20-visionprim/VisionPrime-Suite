# 46 — فاز ۱.۱: تکمیل تمام شکاف‌های باقی‌مانده + تولید تصویر

**تاریخ:** ۲۰۲۶-۰۸-۲۱
**وضعیت:** در حال اجرا
**هدف:** قبل از فاز ۲، تمام استانداردهای SEO کامل پیاده‌سازی بشه

---

## ۱) شکاف‌های شناسایی شده

| # | شکاف | وضعیت فعلی | هدف |
|---|------|-----------|-----|
| ۱ | LSI Keywords | نه سنجش، نه پرامپت دقیق | پرامپت + سنجش ≥ ۵ LSI |
| ۲ | طول مقدمه ≤ ۱۰۰ کلمه | فقط "۲-۳ خط" در پرامپت | سنجش دقیق + محدودیت پرامپت |
| ۳ | Bold/Strong ≥ ۳ | در پرامپت هست ولی سنجش نیست | سنجش تعداد bold |
| ۴ | URL/Slug validation | تولید میشه ولی validate نمیشه | اعتبارسنجی طول + محتوا |
| ۵ | Table of Contents | نه پرامپت، نه سنجش | پرامپت + سنجش |
| ۶ | جملات فعال ≥ ۷۰٪ | پیاده نشده | پرامپت + سنجش |
| ۷ | یکتایی ≥ ۸۰٪ | پیاده نشده | مقایسه با محتوای ذخیره شده |
| ۸ | تصویر + Alt Text | AI تصویر تولید نمیکنه | پیشنهاد تصویر + alt text |

---

## ۲) نقشه اجرای تسک‌های اتمیک

### بخش A: تقویت پرامپت AI (AiGateway)

| # | تسک | فایل | توضیح |
|---|------|------|-------|
| A.1 | اضافه کردن دستور LSI به پرامپت | AiGateway.php | "حداقل ۵ کلمه LSI مرتبط در متن پراکنده کن" |
| A.2 | اضافه کردن دستور TOC | AiGateway.php | "در ابتدای مقاله فهرست مطالب با anchor اضافه کن" |
| A.3 | محدودیت طول مقدمه | AiGateway.php | "مقدمه حداکثر ۱۰۰ کلمه (۳-۴ خط) باشد" |
| A.4 | تقویت دستور Bold | AiGateway.php | "کلیدواژه‌ها و نکات کلیدی حتماً bold شوند (≥ ۵ بار)" |
| A.5 | دستور جملات فعال | AiGateway.php | "از جملات فعال استفاده کن (≥ ۷۰٪ جملات فعال)" |
| A.6 | دستور پیشنهاد تصویر | AiGateway.php | "برای هر H2 اصلی یک پیشنهاد تصویر + alt text بده" |
| A.7 | خروجی JSON ساختاریافته | AiGateway.php | مقاله + metadata در یک JSON |

### بخش B: تقویت سنجش کیفیت (ContentQualityGuard)

| # | تسک | فایل | توضیح |
|---|------|------|-------|
| B.1 | سنجش LSI | ContentQualityGuard.php | شمارش کلمات مرتبط معنایی غیر از کلیدواژه اصلی |
| B.2 | سنجش طول مقدمه | ContentQualityGuard.php | بررسی پاراگراف اول ≤ ۱۰۰ کلمه |
| B.3 | سنجش Bold/Strong | ContentQualityGuard.php | شمارش تگ‌های <strong>/<b> |
| B.4 | سنجش TOC | ContentQualityGuard.php | بررسی وجود فهرست مطالب در ابتدا |
| B.5 | سنجش جملات فعال | ContentQualityGuard.php | تحلیل ساختار جملات (فاعل + مفعول) |
| B.6 | سنجش تصویر + Alt | ContentQualityGuard.php | شمارش تگ <img> + بررسی alt |
| B.7 | سنجش یکتایی | ContentQualityGuard.php | مقایسه با content_drafts موجود |

### بخش C: اعتبارسنجی URL/Slug

| # | تسک | فایل | توضیح |
|---|------|------|-------|
| C.1 | متد validateSlug() | ContentApiController.php | بررسی طول ≤ ۷۵، شامل کلیدواژه، بدون کاراکتر خاص |
| C.2 | اعمال در generate | ContentApiController.php | اگر slug نامعتبر بود، خودکار اصلاح |

### بخش D: تولید تصویر (Phase 1.1)

| # | تسک | فایل | توضیح |
|---|------|------|-------|
| D.1 | ImageSuggestionService | جدید | تولید پیشنهاد تصویر برای هر بخش |
| D.2 | Alt Text Generator | درون ImageSuggestion | تولید alt text SEO شده |
| D.3 | Unsplash/Pexels API | جدید | جستجوی تصویر رایگان مرتبط |
| D.4 | ذخیره پیشنهادات | content_drafts | ذخیره image_suggestions در draft |
| D.5 | UI پیشنهاد تصویر | ArticleDraft/Create.vue | نمایش تصاویر پیشنهادی در مرحله نتیجه |

---

## ۳) ترتیب اجرا

```
مرحله ۱: تقویت پرامپت (A.1-A.7) — ۱۵ دقیقه
مرحله ۲: تقویت سنجش (B.1-B.7) — ۱۵ دقیقه
مرحله ۳: اعتبارسنجی slug (C.1-C.2) — ۵ دقیقه
مرحله ۴: تولید تصویر (D.1-D.5) — ۲۰ دقیقه
مرحله ۵: تست جامع — ۱۰ دقیقه
```

---

## ۴) معیار موفقیت نهایی

| معیار | هدف |
|-------|-----|
| استانداردهای On-Page | ۲۰/۲۰ ✅ |
| E-E-A-T Score | ≥ ۶۰٪ |
| LSI Keywords | ≥ ۵ در محتوا |
| TOC | وجود داشته باشد |
| Bold/Strong | ≥ ۵ بار |
| مقدمه | ≤ ۱۰۰ کلمه |
| جملات فعال | ≥ ۷۰٪ |
| تصویر + Alt | پیشنهاد برای هر H2 |
| URL/Slug | اعتبارسنجی خودکار |
| یکتایی | ≥ ۸۰٪ نسبت به محتوای قبلی |
